# Realtime-canvas
Real-Time Collaborative Drawing Canvas allows multiple users to draw together live on a shared canvas using WebSockets. It provides smooth real-time synchronization of strokes across all connected clients with a simple frontend UI. The project includes a lightweight Node.js server and client-side scripts enabling fast, interactive collaboration.
